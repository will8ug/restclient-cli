## 1. Project Setup

- [ ] 1.1 Initialize Go module (`go mod init github.com/will8ug/restclient-cli`) and create directory structure: `cmd/`, `internal/parser/`, `internal/executor/`, `internal/formatter/`
- [ ] 1.2 Add cobra dependency (`go get github.com/spf13/cobra`) and scaffold main.go with root command
- [ ] 1.3 Create .gitignore additions for Go (binary output) and a minimal README.md

## 2. HTTP File Parser

- [ ] 2.1 Define data types: `Request` struct (Name, Method, URL, Headers, Body), `ParseResult` struct (Variables, Requests, Errors)
- [ ] 2.2 Implement comment stripping — skip lines starting with `#` (but not `###`) or `//`
- [ ] 2.3 Implement file-level variable parsing — extract `@name = value` definitions into a map
- [ ] 2.4 Implement request splitting — split file content by `###` separator, extract optional request name from separator line
- [ ] 2.5 Implement request line parsing — extract method and URL from the first non-blank, non-comment line of each block
- [ ] 2.6 Implement header parsing — extract `Key: Value` lines after the request line, stop at blank line
- [ ] 2.7 Implement body parsing — capture content after the first blank line until next separator or EOF
- [ ] 2.8 Implement variable substitution — replace `{{name}}` in URLs, headers, and bodies; error on undefined variables
- [ ] 2.9 Implement parse error reporting with line numbers
- [ ] 2.10 Write unit tests for all parser requirements (multiple requests, comments, variables, headers, body, error cases)

## 3. Request Executor

- [ ] 3.1 Implement `Execute(request Request, timeout time.Duration) (Response, error)` using `net/http` — send method, URL, headers, body
- [ ] 3.2 Define `Response` struct (StatusCode, Status, Headers, Body, Duration)
- [ ] 3.3 Implement timeout handling with configurable `time.Duration`
- [ ] 3.4 Implement connection error handling with clear messages (DNS, connection refused)
- [ ] 3.5 Configure redirect following (up to 10 hops) and redirect loop detection
- [ ] 3.6 Implement response timing measurement (start-to-finish duration)
- [ ] 3.7 Write unit tests for executor (use httptest for mock server — success, timeout, errors, redirects)

## 4. Response Formatter

- [ ] 4.1 Implement response output: status line, headers, and body to stdout
- [ ] 4.2 Implement JSON pretty-printing for response bodies with `application/json` content type
- [ ] 4.3 Implement verbose mode — print request details (method, URL, headers, body) before the response
- [ ] 4.4 Add color output for status codes (green 2xx, yellow 3xx, red 4xx/5xx) and timing display
- [ ] 4.5 Write unit tests for formatter output

## 5. CLI Commands

- [ ] 5.1 Implement `run` command — accept file path argument, parse file, execute all requests, display responses
- [ ] 5.2 Implement `--request` / `-r` flag for selecting a specific request by name or 1-based index
- [ ] 5.3 Implement `--verbose` / `-v` flag to enable verbose output mode
- [ ] 5.4 Implement `--timeout` / `-t` flag to set request timeout in seconds (default 30)
- [ ] 5.5 Implement `list` command — show all requests in a file with index, name, method, and URL
- [ ] 5.6 Implement `--version` flag and help text (auto-generated by cobra)
- [ ] 5.7 Implement exit codes: 0 for success, 1 for request failures, 2 for parse errors

## 6. Integration & Polish

- [ ] 6.1 Create sample `.http` files in `examples/` directory for manual testing
- [ ] 6.2 Write integration tests — parse and execute against a test server end-to-end
- [ ] 6.3 Verify `go build` produces a working binary and `go test ./...` passes
- [ ] 6.4 Add build instructions and usage examples to README.md
